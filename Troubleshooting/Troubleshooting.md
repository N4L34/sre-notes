[[Troubleshooting. Конспект]]
# Troubleshooting

## Как разбирать каждый кейс

Для каждого кейса проговаривай:

- [ ]  Какой симптом и масштаб проблемы
- [ ]  Что изменилось перед проблемой
- [ ]  На каком уровне искать причину
- [ ]  Какие гипотезы наиболее вероятны
- [ ]  Какие команды и данные проверить
- [ ]  Как быстро восстановить сервис
- [ ]  Как найти Root Cause
- [ ]  Как предотвратить повторение

---

# Уровень 1 — базовые кейсы

## 1. Linux и процессы

- [ ]  Процесс приложения не запущен
- [ ]  Процесс запущен, но не отвечает
- [ ]  Процесс постоянно перезапускается
- [ ]  Процесс завершается сразу после запуска
- [ ]  Сервис не запускается через `systemctl`
- [ ]  Сервис работает вручную, но не работает через systemd
- [ ]  Сервис находится в состоянии `failed`
- [ ]  Процесс использует 100% CPU
- [ ]  Один поток процесса использует 100% CPU
- [ ]  На сервере высокий `load average`
- [ ]  Высокий load average при низком CPU
- [ ]  На сервере почти закончилась RAM
- [ ]  Процесс был убит OOM Killer
- [ ]  Приложение постепенно потребляет всё больше памяти
- [ ]  На сервере активно используется swap
- [ ]  Процесс находится в состоянии `D`
- [ ]  Процесс находится в состоянии Zombie
- [ ]  Процесс невозможно завершить через `kill`
- [ ]  Приложение получает `Permission denied`
- [ ]  Команда работает от root, но не работает от обычного пользователя
- [ ]  Скрипт запускается вручную, но не запускается из cron
- [ ]  После перезагрузки сервис не поднялся
- [ ]  Сервер работает медленно, но CPU и RAM в норме
- [ ]  Приложение не может создать новый процесс
- [ ]  Приложение получает ошибку `Too many open files`

---

## 2. Диск и файловая система

- [ ]  На сервере закончилось место на диске
- [ ]  `df -h` показывает 100%, но большие файлы не находятся
- [ ]  `du` и `df` показывают разный объём занятого места
- [ ]  На диске есть место, но файлы невозможно создать
- [ ]  Закончились inode
- [ ]  Один лог-файл занял весь диск
- [ ]  Логи не ротируются
- [ ]  Удалённый файл продолжает занимать место
- [ ]  Раздел смонтирован в режиме `read-only`
- [ ]  Приложение не может записывать в директорию
- [ ]  После перезагрузки filesystem не смонтировалась
- [ ]  Диск отвечает очень медленно
- [ ]  Высокий `iowait`
- [ ]  На сервере резко вырос Disk I/O
- [ ]  Файловая система повреждена
- [ ]  Закончилось место в `/tmp`
- [ ]  Приложение создаёт огромное количество маленьких файлов
- [ ]  Backup заполнил основной диск
- [ ]  Logrotate удаляет логи слишком рано
- [ ]  Файл существует, но приложение сообщает `No such file or directory`

---

## 3. Базовая сеть

- [ ]  Сервис слушает только на `127.0.0.1`
- [ ]  Сервис не слушает ожидаемый порт
- [ ]  `Connection refused`
- [ ]  `Connection timeout`
- [ ]  `No route to host`
- [ ]  `Network is unreachable`
- [ ]  Сервер доступен по IP, но недоступен по DNS-имени
- [ ]  DNS-имя резолвится в неправильный IP
- [ ]  DNS иногда резолвится, иногда нет
- [ ]  Один сервер доступен, другой в той же сети — нет
- [ ]  Ping работает, но TCP-подключение не устанавливается
- [ ]  TCP работает, но ping не проходит
- [ ]  Порт доступен локально, но недоступен удалённо
- [ ]  Firewall блокирует входящий трафик
- [ ]  Firewall блокирует исходящий трафик
- [ ]  После изменения маршрутов пропал доступ к серверу
- [ ]  Приложение подключается по IPv4, но не по IPv6
- [ ]  Приложение пытается использовать IPv6 вместо IPv4
- [ ]  Соединение работает из одной подсети, но не работает из другой
- [ ]  Соединение периодически обрывается
- [ ]  Между двумя хостами высокая latency
- [ ]  Между двумя хостами packet loss
- [ ]  Большие пакеты не проходят, маленькие проходят
- [ ]  Закончились ephemeral ports
- [ ]  На сервере слишком много соединений в `TIME_WAIT`
- [ ]  На сервере растёт количество `CLOSE_WAIT`

---

## 4. HTTP и HTTPS

- [ ]  Сайт возвращает `400 Bad Request`
- [ ]  Сайт возвращает `401 Unauthorized`
- [ ]  Сайт возвращает `403 Forbidden`
- [ ]  Сайт возвращает `404 Not Found`
- [ ]  Сайт возвращает `405 Method Not Allowed`
- [ ]  Сайт возвращает `413 Request Entity Too Large`
- [ ]  Сайт возвращает `429 Too Many Requests`
- [ ]  Сайт возвращает `500 Internal Server Error`
- [ ]  Сайт возвращает `502 Bad Gateway`
- [ ]  Сайт возвращает `503 Service Unavailable`
- [ ]  Сайт возвращает `504 Gateway Timeout`
- [ ]  HTTP работает, HTTPS не работает
- [ ]  HTTPS работает в браузере, но не работает через `curl`
- [ ]  Сертификат истёк
- [ ]  Сертификат выпущен не на тот домен
- [ ]  Клиент не доверяет Certificate Authority
- [ ]  Сервер отдаёт неполную certificate chain
- [ ]  TLS handshake периодически завершается ошибкой
- [ ]  После HTTP → HTTPS redirect возник redirect loop
- [ ]  GET работает, POST не работает
- [ ]  Маленькие запросы работают, большие не проходят
- [ ]  API работает напрямую, но не работает через proxy
- [ ]  Один URL работает, другой возвращает 404
- [ ]  Приложение возвращает неправильный Content-Type
- [ ]  Запросы зависают до истечения timeout

---

# Уровень 2 — сервисы и инфраструктура

## 5. Nginx и Load Balancing

- [ ]  Nginx не запускается после изменения конфигурации
- [ ]  Nginx запущен, но порт 80 не слушается
- [ ]  Nginx отдаёт стандартную страницу вместо приложения
- [ ]  Nginx выбирает неправильный `server` block
- [ ]  Nginx выбирает неправильный `location`
- [ ]  `proxy_pass` отправляет запросы на неправильный URI
- [ ]  Nginx возвращает 502 для всех upstream-серверов
- [ ]  Nginx возвращает 502 только для одного backend
- [ ]  Nginx возвращает 504 под нагрузкой
- [ ]  Backend отвечает напрямую, но Nginx возвращает 502
- [ ]  Backend получает неправильный `Host` header
- [ ]  Backend не видит реальный IP клиента
- [ ]  WebSocket работает напрямую, но не работает через Nginx
- [ ]  Загрузка большого файла завершается ошибкой
- [ ]  Ответ backend обрывается через определённое время
- [ ]  Один backend получает почти весь трафик
- [ ]  Least Connections распределяет нагрузку неравномерно
- [ ]  Sticky Sessions перестали работать
- [ ]  После падения backend Nginx продолжает отправлять на него запросы
- [ ]  Все backend работают, но Nginx возвращает 503
- [ ]  После reload часть соединений оборвалась
- [ ]  Nginx достиг лимита `worker_connections`
- [ ]  Nginx использует слишком много памяти из-за buffering
- [ ]  Клиенты получают устаревшие данные из proxy cache
- [ ]  Rate Limiting блокирует легитимных пользователей

---

## 6. Docker

- [ ]  Контейнер не запускается
- [ ]  Контейнер сразу переходит в `Exited`
- [ ]  Контейнер постоянно перезапускается
- [ ]  В контейнере не найден исполняемый файл
- [ ]  Приложение работает локально, но не работает в контейнере
- [ ]  Контейнер не может подключиться к интернету
- [ ]  Один контейнер не может обратиться к другому
- [ ]  Контейнер недоступен через опубликованный порт
- [ ]  Порт опубликован, но приложение слушает только localhost
- [ ]  Контейнер не может резолвить DNS
- [ ]  Контейнер не видит переменную окружения
- [ ]  Контейнер получает неправильное значение secret
- [ ]  Контейнер не может записать в volume
- [ ]  Данные исчезли после пересоздания контейнера
- [ ]  Docker volume заполнил диск
- [ ]  Docker logs заняли весь диск
- [ ]  Контейнер был убит из-за нехватки памяти
- [ ]  Контейнер использует весь CPU хоста
- [ ]  Healthcheck постоянно возвращает `unhealthy`
- [ ]  Образ не скачивается из registry
- [ ]  Registry требует авторизацию
- [ ]  Образ работает на одном сервере, но не работает на другом
- [ ]  После обновления образа запускается старая версия
- [ ]  Docker Compose запускает сервисы в неправильном порядке
- [ ]  Контейнер не может подключиться к сервису на хосте

---

## 7. Kubernetes — Pods

- [ ]  Pod находится в состоянии `Pending`
- [ ]  Pod находится в `CrashLoopBackOff`
- [ ]  Pod находится в `ImagePullBackOff`
- [ ]  Pod находится в `ErrImagePull`
- [ ]  Pod находится в `CreateContainerConfigError`
- [ ]  Pod находится в `ContainerCreating`
- [ ]  Pod находится в состоянии `Terminating` слишком долго
- [ ]  Pod завершился со статусом `Error`
- [ ]  Pod был `OOMKilled`
- [ ]  Pod постоянно Evicted
- [ ]  Pod запускается, но не становится Ready
- [ ]  Readiness Probe возвращает ошибку
- [ ]  Liveness Probe перезапускает рабочее приложение
- [ ]  Startup Probe никогда не проходит
- [ ]  Pod работает на одном Node, но не работает на другом
- [ ]  Pod не получает переменные из ConfigMap
- [ ]  Pod не получает данные из Secret
- [ ]  После изменения ConfigMap приложение не увидело изменения
- [ ]  Init Container не завершается
- [ ]  Sidecar Container падает и ломает весь Pod
- [ ]  Приложение работает через `kubectl exec`, но недоступно по сети
- [ ]  `kubectl logs` не показывает ожидаемые логи
- [ ]  Нужные логи исчезли после перезапуска контейнера
- [ ]  Pod не может получить доступ к Kubernetes API
- [ ]  Pod не может записывать в mounted volume

---

## 8. Kubernetes — Scheduling и Resources

- [ ]  Pod не планируется из-за недостатка CPU
- [ ]  Pod не планируется из-за недостатка Memory
- [ ]  Pod не планируется из-за Node Selector
- [ ]  Pod не планируется из-за Node Affinity
- [ ]  Pod не планируется из-за Pod Anti-Affinity
- [ ]  Pod не планируется из-за Taints
- [ ]  Pod попадает на неправильный Node
- [ ]  Все реплики Deployment размещены на одном Node
- [ ]  Requests слишком высокие, хотя фактическое потребление низкое
- [ ]  Limits слишком низкие и приложение throttling
- [ ]  CPU usage низкий, но приложение работает медленно из-за CPU throttling
- [ ]  Node находится в состоянии `NotReady`
- [ ]  На Node возник `DiskPressure`
- [ ]  На Node возник `MemoryPressure`
- [ ]  Pods массово Evicted с одного Node
- [ ]  Cluster Autoscaler не добавляет Node
- [ ]  Node добавился, но Pods всё равно остаются Pending
- [ ]  PodDisruptionBudget мешает обновлению
- [ ]  Drain Node не может завершиться
- [ ]  DaemonSet не запускается на части Nodes

---

## 9. Kubernetes — Services и Networking

- [ ]  Service существует, но приложение недоступно
- [ ]  Service не имеет Endpoints
- [ ]  Service выбирает неправильные Pods
- [ ]  `targetPort` указан неправильно
- [ ]  ClusterIP работает из одного Pod, но не работает из другого
- [ ]  Pod доступен по IP, но недоступен через Service
- [ ]  Service доступен внутри кластера, но недоступен снаружи
- [ ]  NodePort недоступен
- [ ]  LoadBalancer остаётся в состоянии `Pending`
- [ ]  Ingress возвращает 404
- [ ]  Ingress возвращает 502
- [ ]  Ingress возвращает 503
- [ ]  Один Ingress перехватывает трафик другого
- [ ]  TLS в Ingress использует неправильный сертификат
- [ ]  Pod не может резолвить имя Service
- [ ]  CoreDNS Pods находятся в CrashLoopBackOff
- [ ]  DNS работает для одних namespace, но не работает для других
- [ ]  DNS-запросы в кластере работают медленно
- [ ]  NetworkPolicy блокирует трафик между Pods
- [ ]  После включения NetworkPolicy пропал DNS
- [ ]  Pod может подключиться по IP, но не может по имени
- [ ]  kube-proxy не создаёт необходимые правила
- [ ]  CNI Plugin не создаёт сеть для Pod
- [ ]  Между Nodes не проходит Pod-to-Pod трафик
- [ ]  MTU приводит к зависанию отдельных HTTP-запросов

---

## 10. Kubernetes — Deployments

- [ ]  Deployment создал меньше реплик, чем указано
- [ ]  Deployment rollout завис
- [ ]  Новые Pods не становятся Ready
- [ ]  Deployment удаляет старые Pods слишком рано
- [ ]  После rollout пользователи получают ошибки
- [ ]  `kubectl rollout undo` не восстанавливает приложение
- [ ]  Новая версия использует старый ConfigMap
- [ ]  Deployment запускает старый Docker image
- [ ]  Использование тега `latest` не обновляет приложение
- [ ]  ReplicaSet продолжает создавать падающие Pods
- [ ]  Rolling Update вызывает нехватку ресурсов
- [ ]  Recreate strategy вызывает downtime
- [ ]  HPA не увеличивает количество реплик
- [ ]  HPA постоянно увеличивает и уменьшает количество Pods
- [ ]  HPA показывает `unknown` metrics
- [ ]  StatefulSet обновляет Pods не в ожидаемом порядке
- [ ]  StatefulSet Pod не может подключить старый volume
- [ ]  Job никогда не завершается
- [ ]  CronJob запускает несколько копий одновременно
- [ ]  CronJob не запускается по расписанию

---

## 11. Kubernetes — Storage

- [ ]  PVC находится в состоянии `Pending`
- [ ]  PV существует, но не привязывается к PVC
- [ ]  StorageClass указан неправильно
- [ ]  Dynamic Provisioning не создаёт volume
- [ ]  Pod не может примонтировать volume
- [ ]  Volume уже используется другим Node
- [ ]  Приложение получает `Read-only file system`
- [ ]  Несколько Pods не могут одновременно использовать volume
- [ ]  После пересоздания StatefulSet пропали данные
- [ ]  Новый Pod подключил пустой volume
- [ ]  Volume закончился по размеру
- [ ]  Расширение PVC не изменило доступный размер
- [ ]  CSI Driver не работает
- [ ]  Storage backend недоступен
- [ ]  Volume работает медленно и увеличивает latency приложения

---

## 12. CI/CD

- [ ]  Pipeline не запускается после commit
- [ ]  Pipeline запускается не для той ветки
- [ ]  Runner недоступен
- [ ]  Job остаётся в состоянии Pending
- [ ]  Job работает локально, но падает в CI
- [ ]  В CI отсутствует нужная переменная
- [ ]  Secret отображается в логах pipeline
- [ ]  Pipeline не может скачать зависимости
- [ ]  Cache содержит устаревшие зависимости
- [ ]  Artifact не передаётся в следующий stage
- [ ]  Build Docker image завершается ошибкой
- [ ]  Pipeline не может выполнить push в registry
- [ ]  Deploy job не имеет доступа к Kubernetes
- [ ]  Pipeline завершился успешно, но приложение не обновилось
- [ ]  Pipeline развернул приложение не в тот environment
- [ ]  Production deployment запустился из feature branch
- [ ]  Два pipeline одновременно деплоят разные версии
- [ ]  Migration базы упала во время deployment
- [ ]  Rollback приложения несовместим с новой схемой базы
- [ ]  Manual job запустил пользователь без нужных прав

---

## 13. GitOps и Argo CD

- [ ]  Argo CD показывает `OutOfSync`
- [ ]  Argo CD показывает `Degraded`
- [ ]  Argo CD показывает `Missing`
- [ ]  Argo CD не видит новый commit
- [ ]  Argo CD не может получить доступ к Git repository
- [ ]  Argo CD не может подключиться к Kubernetes cluster
- [ ]  Sync завершается ошибкой
- [ ]  Auto Sync не запускается
- [ ]  Self Heal постоянно откатывает ручные изменения
- [ ]  Application зависла на удалении
- [ ]  Helm template в Argo CD отличается от локального
- [ ]  Secret отсутствует после Sync
- [ ]  Sync Wave запускает ресурсы в неправильном порядке
- [ ]  Argo CD постоянно пересоздаёт один ресурс
- [ ]  Rollback в Argo CD не восстанавливает рабочее состояние

---

## 14. Prometheus и Grafana

- [ ]  Prometheus Target находится в состоянии `Down`
- [ ]  Prometheus не обнаруживает новый target
- [ ]  `/metrics` доступен вручную, но scrape не работает
- [ ]  Scrape завершается timeout
- [ ]  Prometheus получает TLS error
- [ ]  ServiceMonitor существует, но target не появился
- [ ]  ServiceMonitor выбирает неправильный Service
- [ ]  Метрика существует, но PromQL ничего не возвращает
- [ ]  `rate()` показывает неожиданный результат
- [ ]  Grafana показывает `No data`
- [ ]  Grafana Data Source недоступен
- [ ]  Dashboard показывает неправильные значения
- [ ]  Dashboard работает медленно
- [ ]  Prometheus использует слишком много памяти
- [ ]  Prometheus заполнил диск
- [ ]  После изменения labels резко выросла cardinality
- [ ]  Recording Rule не вычисляется
- [ ]  Alert Rule остаётся в состоянии Pending
- [ ]  Alert должен сработать, но не появляется
- [ ]  Метрики Kubernetes пропали после обновления monitoring stack

---

## 15. Alertmanager

- [ ]  Prometheus создаёт alert, но Alertmanager его не получает
- [ ]  Alertmanager получает alert, но уведомление не приходит
- [ ]  Alert отправляется не той команде
- [ ]  Critical alert попадает в warning receiver
- [ ]  Один alert приходит десятки раз
- [ ]  Несколько одинаковых alerts не группируются
- [ ]  Alert приходит с большой задержкой
- [ ]  Resolved notification не приходит
- [ ]  Silence не подавляет alert
- [ ]  Silence подавляет слишком много alerts
- [ ]  Inhibition Rule не работает
- [ ]  Недоступность одного receiver блокирует остальные
- [ ]  Webhook receiver возвращает ошибку
- [ ]  После reload Alertmanager использует старую конфигурацию
- [ ]  Два экземпляра Alertmanager отправляют дублирующиеся уведомления

---

## 16. Logging, ELK и Loki

- [ ]  Приложение пишет логи, но они не попадают в хранилище
- [ ]  Логи одного Pod отсутствуют
- [ ]  Логи всех новых Pods отсутствуют
- [ ]  Fluent Bit не может читать container logs
- [ ]  Fluent Bit не может отправить данные в Elasticsearch
- [ ]  Log agent использует слишком много CPU
- [ ]  Log agent использует слишком много памяти
- [ ]  Неправильно парсятся JSON-логи
- [ ]  Timestamp логов отображается неправильно
- [ ]  Логи приходят с задержкой
- [ ]  Логи дублируются
- [ ]  Elasticsearch Cluster находится в состоянии Yellow
- [ ]  Elasticsearch Cluster находится в состоянии Red
- [ ]  Elasticsearch не размещает shards
- [ ]  На Elasticsearch закончилось место
- [ ]  Создано слишком много маленьких indices
- [ ]  Kibana не видит новый index
- [ ]  Loki не принимает новые логи
- [ ]  Loki query работает слишком медленно
- [ ]  Loki labels создали высокую cardinality
- [ ]  Grafana Explore показывает логи не всех Pods
- [ ]  После restart log agent часть логов потерялась
- [ ]  Один огромный stack trace ломает parsing pipeline
- [ ]  В логах случайно появился secret
- [ ]  Retention Policy удалила необходимые для расследования логи

---

# Уровень 3 — сложные связанные кейсы

## 17. Приложение и база данных

- [ ]  Приложение не может подключиться к базе после restart
- [ ]  База доступна вручную, но приложение получает timeout
- [ ]  Приложение исчерпало connection pool
- [ ]  База достигла максимального количества connections
- [ ]  После роста трафика база стала отвечать медленно
- [ ]  Один SQL-запрос создаёт высокую нагрузку
- [ ]  База использует 100% CPU
- [ ]  На базе закончился диск
- [ ]  Реплика базы сильно отстаёт
- [ ]  Приложение читает устаревшие данные с replica
- [ ]  После failover приложение продолжает обращаться к старому primary
- [ ]  DNS базы изменился, но приложение использует старый IP
- [ ]  Migration заблокировала таблицу
- [ ]  Deployment завис из-за долгой migration
- [ ]  Новая версия приложения несовместима со старой схемой базы
- [ ]  Rollback приложения невозможен после migration
- [ ]  Backup базы оказался повреждён
- [ ]  Restore работает, но часть данных отсутствует
- [ ]  База доступна из одного namespace, но недоступна из другого
- [ ]  TLS-сертификат базы истёк

---

## 18. Производительность

- [ ]  Latency выросла, но CPU и Memory в норме
- [ ]  CPU вырос только на одном Pod
- [ ]  Все Pods работают нормально, но пользователи видят задержки
- [ ]  Средняя latency нормальная, но p99 очень высокая
- [ ]  Latency растёт только при определённом типе запросов
- [ ]  Ошибок нет, но throughput снизился
- [ ]  После масштабирования Pods производительность ухудшилась
- [ ]  Добавление реплик не увеличивает пропускную способность
- [ ]  Один downstream-сервис замедляет всю систему
- [ ]  Внешний API иногда отвечает по 20 секунд
- [ ]  Retry увеличили нагрузку и ухудшили инцидент
- [ ]  Timeout клиента меньше timeout backend
- [ ]  Connection Pool слишком маленький
- [ ]  Connection Pool слишком большой и перегружает базу
- [ ]  Garbage Collector вызывает периодические паузы
- [ ]  CPU throttling происходит при низком среднем CPU
- [ ]  Большая очередь запросов растёт быстрее обработки
- [ ]  Cache hit rate резко снизился
- [ ]  После очистки cache база перегрузилась
- [ ]  Один пользователь создаёт непропорционально большую нагрузку

---

## 19. Частичные отказы

- [ ]  Проблема возникает только у части пользователей
- [ ]  Проблема возникает только в одном регионе
- [ ]  Проблема возникает только у мобильных клиентов
- [ ]  Проблема возникает только по IPv6
- [ ]  Проблема возникает только для одного tenant
- [ ]  Проблема возникает только для новых аккаунтов
- [ ]  Проблема возникает только после авторизации
- [ ]  Один из пяти Pods возвращает ошибки
- [ ]  Ошибки возникают только на одном Node
- [ ]  Ошибки возникают только через один Load Balancer
- [ ]  Один Availability Zone отвечает медленно
- [ ]  Один DNS resolver возвращает неправильный адрес
- [ ]  Один shard Elasticsearch недоступен
- [ ]  Часть сообщений теряется в очереди
- [ ]  Проблема проявляется только при больших request body
- [ ]  Проблема возникает только при использовании sticky session
- [ ]  Проблема возникает только после истечения access token
- [ ]  Один экземпляр приложения использует старую конфигурацию
- [ ]  Часть Pods использует старый Secret
- [ ]  Canary версия работает хуже stable версии

---

## 20. Очереди и асинхронные задачи

- [ ]  Producer отправляет сообщения, но Consumer их не обрабатывает
- [ ]  Очередь сообщений постоянно растёт
- [ ]  Consumer работает, но lag увеличивается
- [ ]  Сообщения обрабатываются несколько раз
- [ ]  Часть сообщений теряется
- [ ]  Dead Letter Queue быстро заполняется
- [ ]  Один плохой message блокирует partition
- [ ]  Consumer Group потеряла часть consumers
- [ ]  После restart Consumer начал обработку старых сообщений
- [ ]  Retry вызывает бесконечный цикл
- [ ]  Порядок сообщений нарушен
- [ ]  Сообщения одного клиента блокируют остальных
- [ ]  Broker достиг лимита диска
- [ ]  Broker доступен, но publish завершается timeout
- [ ]  Слишком большое сообщение не принимается
- [ ]  Под нагрузкой растёт время обработки background jobs
- [ ]  CronJob создаёт задачи быстрее, чем workers их обрабатывают
- [ ]  Worker завершает задачу, но не подтверждает message
- [ ]  После масштабирования workers база перегрузилась
- [ ]  Очередь работает, но метрики lag отсутствуют

---

## 21. Cloud и Infrastructure as Code

- [ ]  VM создана, но SSH недоступен
- [ ]  VM имеет Public IP, но порт закрыт
- [ ]  Security Group разрешает трафик, но соединение не работает
- [ ]  Route Table не содержит маршрут в интернет
- [ ]  Private VM не может скачать обновления
- [ ]  NAT Gateway существует, но исходящий трафик не работает
- [ ]  Load Balancer считает все targets unhealthy
- [ ]  Load Balancer работает, но backend не видит реальный IP
- [ ]  Autoscaling создаёт VM, которые не проходят health check
- [ ]  Новая VM не регистрируется в Load Balancer
- [ ]  Instance Role не даёт доступ к Object Storage
- [ ]  Object Storage Bucket случайно стал публичным
- [ ]  Приложение получает `Access Denied` от cloud API
- [ ]  Cloud quota не позволяет создать новые ресурсы
- [ ]  Terraform plan хочет пересоздать production resource
- [ ]  Terraform state не соответствует реальной инфраструктуре
- [ ]  Terraform state заблокирован
- [ ]  Два pipeline одновременно изменяют Terraform state
- [ ]  Ресурс был удалён вручную, но остался в state
- [ ]  Terraform применился частично
- [ ]  Ansible Playbook работает на части серверов
- [ ]  Ansible Task не является idempotent
- [ ]  Новый сервер не попал в inventory
- [ ]  Cloud credentials истекли во время deployment
- [ ]  Удаление Terraform resource привело к потере данных

---

## 22. Security и Access

- [ ]  Пользователь внезапно потерял доступ к production
- [ ]  Service Account получает `Forbidden`
- [ ]  Kubernetes Role существует, но доступ не работает
- [ ]  RoleBinding указывает на неправильный namespace
- [ ]  IAM Policy выглядит правильной, но запрос запрещён
- [ ]  Explicit Deny перекрывает Allow
- [ ]  Временные credentials истекли
- [ ]  SSH перестал принимать ключ
- [ ]  Private key имеет неправильные permissions
- [ ]  После ротации secret приложение перестало работать
- [ ]  Часть Pods использует старую версию secret
- [ ]  Vault недоступен, новые Pods не запускаются
- [ ]  Vault token истёк
- [ ]  Сертификат mTLS истёк
- [ ]  Клиентский сертификат выпущен неправильным CA
- [ ]  NetworkPolicy разрешает приложение, но блокирует DNS
- [ ]  SELinux блокирует доступ приложения к файлу
- [ ]  AppArmor блокирует системный вызов контейнера
- [ ]  Secret попал в Git repository
- [ ]  API token появился в логах
- [ ]  Скомпрометированный token продолжает работать после ротации
- [ ]  У пользователя остались права после увольнения
- [ ]  CI Runner получил слишком широкие cloud permissions
- [ ]  Production Pod запущен в privileged mode
- [ ]  Неизвестный пользователь изменил IAM Policy

---

# Уровень 4 — полноценные production-инциденты

## 23. Инциденты после релиза

- [ ]  После релиза количество 5xx выросло с 0,1% до 15%
- [ ]  После релиза latency выросла в три раза без роста CPU
- [ ]  После релиза часть пользователей не может авторизоваться
- [ ]  После релиза новые Pods не становятся Ready
- [ ]  После релиза старые Pods удалились, а новые не запустились
- [ ]  После релиза выросло количество DB connections
- [ ]  После релиза база начала использовать 100% CPU
- [ ]  После релиза очередь сообщений начала быстро расти
- [ ]  После релиза вырос объём логов в 50 раз
- [ ]  После релиза Prometheus начал использовать всю память
- [ ]  После релиза HPA масштабировал приложение до максимума
- [ ]  После релиза только один endpoint стал медленным
- [ ]  После релиза приложение работает, но background jobs остановились
- [ ]  После релиза часть Pods использует новую версию, часть старую
- [ ]  Rollback вернул приложение, но проблема осталась

---

## 24. Массовые отказы

- [ ]  Все сервисы одновременно начали возвращать 502
- [ ]  Все Pods в кластере потеряли доступ к DNS
- [ ]  Все новые Pods остаются в состоянии Pending
- [ ]  Все приложения потеряли доступ к базе
- [ ]  Все Prometheus targets одновременно стали Down
- [ ]  Все HTTPS-сервисы начали возвращать ошибки сертификата
- [ ]  Все CI/CD pipelines перестали работать
- [ ]  Все Nodes одного Availability Zone стали NotReady
- [ ]  Все приложения потеряли доступ к Secret Manager
- [ ]  Все исходящие запросы из private subnet перестали работать
- [ ]  Все пользователи были разлогинены
- [ ]  Все API-запросы начали получать 429
- [ ]  Логи со всего кластера перестали поступать
- [ ]  Все Pods начали перезапускаться из-за liveness probe
- [ ]  После обновления CNI пропала межсервисная сеть

---

## 25. Перегрузка системы

- [ ]  После резкого роста трафика сервис начал возвращать 503
- [ ]  CPU всех Pods достиг 100%, HPA не сработал
- [ ]  HPA сработал, но новые Pods остаются Pending
- [ ]  Новые Pods запустились, но база не выдержала рост connections
- [ ]  Retries создали retry storm
- [ ]  Очередь запросов продолжает расти после снижения трафика
- [ ]  Rate Limiting блокирует всех пользователей
- [ ]  Cache перестал отвечать, весь трафик пошёл в базу
- [ ]  После истечения cache одновременно обновляются миллионы ключей
- [ ]  Один тяжёлый endpoint перегружает весь сервис
- [ ]  Логи под нагрузкой заполняют диск быстрее обработки запросов
- [ ]  Load Balancer продолжает отправлять трафик перегруженным Pods
- [ ]  Readiness Probe проходит, хотя приложение уже не может обслуживать запросы
- [ ]  Масштабирование приложения увеличивает нагрузку на downstream
- [ ]  После восстановления downstream накопившиеся запросы снова его перегружают

---

## 26. Сложные сетевые инциденты

- [ ]  Сервис периодически недоступен ровно на 30 секунд
- [ ]  Соединения обрываются только при передаче больших файлов
- [ ]  Один сервис доступен по HTTP, но недоступен по gRPC
- [ ]  TLS handshake работает внутри кластера, но не работает снаружи
- [ ]  Часть клиентов получает старый IP после DNS-миграции
- [ ]  DNS TTL уменьшили, но клиенты продолжают использовать старый IP
- [ ]  Один Load Balancer работает, второй возвращает 502
- [ ]  TCP handshake проходит, но сервер не отправляет данные
- [ ]  SYN-пакеты доходят до сервера, SYN-ACK не возвращается клиенту
- [ ]  Пакеты проходят в одну сторону, но не возвращаются обратно
- [ ]  Проблема возникает только через VPN
- [ ]  После изменения MTU перестали работать отдельные API-запросы
- [ ]  NAT таблица переполнена
- [ ]  На Node исчерпался conntrack table
- [ ]  Один и тот же IP периодически ведёт на разные версии приложения

---

## 27. Неочевидные инциденты

- [ ]  Метрики показывают норму, но пользователи массово жалуются
- [ ]  Алерты отсутствуют, хотя сервис полностью недоступен
- [ ]  Dashboard зелёный из-за неправильно настроенного PromQL
- [ ]  Логи приложения не содержат ошибок, но запросы возвращают 500
- [ ]  Ошибка возникает только один раз в несколько часов
- [ ]  Проблема исчезает сразу после подключения инженера
- [ ]  Restart временно решает проблему, но она возвращается
- [ ]  Проблема возникает только в production
- [ ]  Один и тот же build работает по-разному на разных Nodes
- [ ]  Проблема зависит от времени суток
- [ ]  Проблема возникает только после нескольких дней uptime
- [ ]  Сервис ломается после ротации сертификата
- [ ]  Сервис ломается после автоматической ротации secret
- [ ]  Проблема проявляется только при переключении primary базы
- [ ]  Неправильное системное время ломает authentication
- [ ]  NTP рассинхронизация вызывает ошибки TLS и tokens
- [ ]  Пользователи получают данные других пользователей
- [ ]  Сервис возвращает успешный статус, но операция фактически не выполняется
- [ ]  Monitoring показывает старые данные как актуальные
- [ ]  Deployment считается успешным, хотя трафик продолжает идти на старую версию

---

## 28. Disaster Recovery

- [ ]  Production database случайно удалена
- [ ]  Повреждён основной Terraform state
- [ ]  Удалён Kubernetes namespace
- [ ]  Удалён production Secret
- [ ]  Удалён Object Storage Bucket
- [ ]  Backup существует, но restore никогда не проверялся
- [ ]  Последний рабочий backup старше допустимого RPO
- [ ]  Весь Kubernetes cluster недоступен
- [ ]  Потерян один Availability Zone
- [ ]  Потерян основной регион
- [ ]  Реплика базы содержит повреждённые данные
- [ ]  Ошибка приложения массово испортила данные
- [ ]  Rollback кода не исправляет повреждённые данные
- [ ]  GitOps автоматически восстанавливает неправильную конфигурацию
- [ ]  Во время восстановления истекли cloud credentials

---

# Финальные интервью-кейсы без явного симптома

- [ ]  Пользователи говорят: «Сайт иногда тормозит»
- [ ]  Разработчик говорит: «Это проблема инфраструктуры»
- [ ]  Инфраструктурная команда говорит: «Это проблема приложения»
- [ ]  Вчера всё работало, сегодня не работает
- [ ]  Никаких изменений не было, но сервис сломался
- [ ]  После restart всё заработало
- [ ]  Проблема возникает только у одного клиента
- [ ]  Проблему невозможно воспроизвести
- [ ]  Есть только один скриншот ошибки без логов
- [ ]  Мониторинг недоступен одновременно с production
- [ ]  Логи были удалены до начала расследования
- [ ]  Нет доступа к проблемному серверу
- [ ]  Нет документации по сервису
- [ ]  Инженер, который разворачивал систему, недоступен
- [ ]  Неизвестно, какие изменения были сделаны вручную
- [ ]  Инцидент затрагивает production, но rollback считается рискованным
- [ ]  Есть несколько возможных причин, но каждая проверка опасна
- [ ]  Исправление одного компонента ухудшает состояние другого
- [ ]  Пользователи жалуются, но бизнес-метрики выглядят нормально
- [ ]  Технические метрики выглядят плохо, но пользователи не жалуются